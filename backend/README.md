# Task Manager Backend

Node.js + Express backend for the task manager with JWT auth and MongoDB.

- Start with `npm install`
- Copy `.env.example` to `.env` and edit.
- Run `npm run dev` (needs nodemon) or `npm start`.
